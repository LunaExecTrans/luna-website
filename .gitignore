# Dependencies
node_modules/

# OS / Editor noise
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/

# Logs / runtime
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Env / secrets
.env
.env.local
.env.*.local
firebase-service-account*.json
*.key
*.pem

# Build / caches (if we ever add them)
dist/
.cache/
.tmp/

# Local backups / scratch
*-backup*.json
rules-backup-*.json
/tmp/
.preview/

# Heavy ORIGINAL video assets — 21-40MB raw captures. Kept local
# only, compressed derivatives live in assets/compressed/ and ARE
# committed so Railway can serve them until R2 is wired up.
assets/*.mp4
assets/*.webm
assets/*.mov
